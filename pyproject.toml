[project]
name = "kf-agent"
version = "0.1.0"
description = "Agentic application for user-friendly basic Kubeflow interaction"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["apps/*", "libs/*"]
